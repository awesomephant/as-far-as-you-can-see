<!doctype html>
<html lang="en">
<head>
    {% include meta.liquid %}
    <link rel="stylesheet" href="/{{'style.css' | url}}">
</head>

<body class='{{class}}'>
    <header class="site--header">
        <h1 class="site--title">
            <a href="/">{{settings.site_title}}</a>
        </h1>
    </header>

    {{ content | safe }}

    <footer class="site-footer">
        ©{{"now" | date: "%Y")}}
    </footer>

    <script type="text/javascript" src="{{'/dist/main.js' | url}}"></script>
</body>

</html>